# Engineering Directives — Order Control System

> هذا الملف قواعد عمل ثابتة (Rules), مش اقتراحات. أي كود أو قرار في المشروع لازم يلتزم بيها. لو تعليمة هنا بتتعارض مع طريقة أسرع أو أسهل، **القاعدة هنا لها الأولوية**.

---

## 0. الملف الأهم: PROJECT_LOG.md

من أول سطر كود، لازم يتعمل ملف `PROJECT_LOG.md` في جذر المشروع، ويتحدث **في نفس اللحظة** اللي أي حاجة من دي بتحصل:
- إنشاء ملف جديد أو Component أو API route
- أي تعديل على `prisma/schema.prisma`
- أي قرار معماري (اخترت مكتبة، غيرت طريقة، رفضت اقتراح)
- أي إجابة توصل من العميل على سؤال معلّق

**ممنوع** إن الملف يتحدث "بعدين" أو "في نهاية الجلسة" — التحديث جزء من نفس الخطوة اللي بتتعمل، مش خطوة منفصلة بعدها.

### الشكل المطلوب لكل Entry في PROJECT_LOG.md
```markdown
## [YYYY-MM-DD] <عنوان قصير للتغيير>
**النوع:** Schema / Feature / Decision / Bug Fix / Client Answer
**اللي اتعمل:** <وصف مختصر>
**السبب:** <ليه اتاخد القرار ده — مش بس إيه هو>
**الملفات المتأثرة:** <قائمة المسارات>
**تأثير على أجزاء تانية:** <لو في، وإلا اكتب "لا يوجد">
```

هذا يشتغل كـ **Architecture Decision Record (ADR)** مبسّط — الهدف إن أي حد (أنا، أو حتى موديل تاني لاحقًا) يقدر يفهم "ليه الكود كده" من غير ما يسأل من الصفر.

---

## 1. لا تبدأ أي تعديل على الـ Schema بدون تأكيد

الأسئلة الخمسة المعلّقة (Brands, Zone pricing columns, نوع عمولة التطبيقات, تخزين بيانات العميل, هيكل المنيو/Variants) **لسه من غير رد من العميل**.

- أي جزء من الـ Schema مرتبط بسؤال معلّق (`Brand`, `DeliveryZone.customerFee/riderFee`, `Platform.commissionRate`) يتبنى بأبسط شكل ممكن (Nullable / Optional) لحد ما يوصل الرد.
- **ممنوع** كتابة Migration نهائية على جدول فيه بيانات حقيقية بناءً على افتراض. لو محتاج تكمل شغل قبل الرد، اشتغل على نسخة تجريبية (seed data وهمي)، مش بيانات العميل الحقيقية من الشيت.
- أول ما يوصل رد العميل، أول خطوة هي تحديث `PROJECT_LOG.md` بالإجابة ("Client Answer")، قبل أي تعديل كود.

---

## 2. Separation of Concerns — إجباري

- **منطق الأعمال لا يُكتب داخل الـ UI Components.** أي حساب (صافي اليوم، شروط الخصم، قواعد الانتقال بين حالات الأوردر) يُكتب في طبقة منفصلة: `src/lib/` أو `src/services/`.
- الـ Component بيستدعي الـ function، مش بيحسب هو نفسه.
- مثال ملموس: حساب `netCash` في Daily Closing يتعمل في `src/lib/closing.ts`، والصفحة (`closing/page.tsx`) بس بتعرض النتيجة وتستدعي الدالة.

---

## 3. Single Source of Truth لكل قاعدة عمل حساسة

القواعد دي تتكتب **مرة واحدة بس**، وكل مكان في النظام يستخدمها، مش يعيد كتابتها:

| القاعدة | مكانها الوحيد |
|---|---|
| الانتقالات المسموحة بين حالات الأوردر (State Machine) | `src/lib/orderStateMachine.ts` |
| تسجيل أي Create/Update/Cancel في الـ Audit Log | `src/lib/audit.ts` — يُستدعى تلقائيًا من كل عملية كتابة على قاعدة البيانات، مش بشكل يدوي في كل صفحة |
| صلاحيات الأدوار (مين يعمل إيه) | `src/lib/auth.ts` (`requireRole(...)`) |

**قاعدة صارمة:** أي عملية `UPDATE` أو `DELETE` على جدول `Order` لازم تعدي من خلال دالة مركزية بتسجل في `AuditLog` تلقائيًا — ممنوع تعديل مباشر على الجدول من أي مكان في الكود بدون المرور بالطبقة دي.

---

## 4. Fail-Safe Defaults

- **لا يوجد Hard Delete لأي Order أبدًا.** فقط `status = CANCELLED` مع `cancelReason` إجباري. هذا قرار نهائي غير قابل للتفاوض، لأنه أساس حل مشكلة الثقة اللي بنينا عليها المشروع كله.
- أي خصم (`discount`) لازم يترفض في الـ Backend لو مفيش `discountReason` + موافقة Manager/Owner مسجلة — مش بس إخفاء الزرار في الـ UI. الـ validation في الـ API نفسه، مش بس في الفورم.
- كل Endpoint بيرجع بيانات حساسة (تقارير، Audit Log) يتحقق من الـ Role في الـ Backend، مش بس بيخفي الرابط في الـ Sidebar.

---

## 5. ترتيب التنفيذ

1. Schema أولي (Nullable حيث فيه غموض) + Project Setup
2. Auth + Roles
3. Menu (بسيط الأول، حتى لو هيتضاف Variants بعدين)
4. **Orders + Audit Log مع بعض من أول Commit** — ممنوع تأجيل الـ Audit Log لمرحلة لاحقة
5. Delivery (Drivers + Zones)
6. Expenses
7. Daily Closing
8. Reports
9. User Management

لا تنتقل لمرحلة جديدة قبل ما تتأكد إن المرحلة اللي قبلها بتحقق بند "Verification Plan" الخاص بيها.

---

## 6. أسلوب التواصل المطلوب من Antigravity

- قبل أي تعديل على `schema.prisma`: اشرح **ليه** التغيير محتاج، مش بس اعمله.
- لو واجهت قرار مش موجود في هذا الملف ولا في `implementation_plan.md`، **اسأل بدل ما تفترض**، وسجل السؤال في `PROJECT_LOG.md` تحت قسم "Open Questions".
- في نهاية أي جلسة عمل، لخّص في `PROJECT_LOG.md`: إيه اللي خلص، إيه الناقص، وإيه أول حاجة في الجلسة الجاية.
